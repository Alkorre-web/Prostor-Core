<!-- 🔹 Базовая структура шаблона -->
{% extends 'base.html' %}

{% block title %}Заголовок страницы{% endblock %}

{% block content %}
  <h1>Привет, {{ user.display_name }}!</h1>

  <!-- Карточка -->
  <article class="card">
    <h3>Навыки</h3>
    <ul class="skills-list">
      {% for skill in skills %}
      <li class="skill-item" style="--accent: {{ skill.color }}">
        <span>{{ skill.icon }}</span>
        <span>{{ skill.name }}</span>
        <span>Ур. {{ skill.level }}</span>
      </li>
      {% else %}
      <li class="empty">Нет навыков</li>
      {% endfor %}
    </ul>
  </article>
{% endblock %}

<!-- 🔹 Условные блоки -->
{% if tasks %}
  <p>Задач: {{ tasks|length }}</p>
{% else %}
  <p>Задач нет</p>
{% endif %}

<!-- 🔹 Циклы -->
{% for task in tasks[:8] %}
  <div class="task {% if task.is_completed %}done{% endif %}">
    {{ task.title }}
  </div>
{% endfor %}

<!-- 🔹 Подключение CSS/JS -->
<link rel="stylesheet" href="{{ url_for('static', filename='css/main.css') }}">
<script src="{{ url_for('static', filename='js/help.js') }}"></script>